import {NextResponse} from 'next/server';import {db} from '@/lib/db';export const dynamic='force-dynamic';export async function GET(){return NextResponse.json({success:true,data:db.prepare('SELECT * FROM settings WHERE id=1').get()})}export async function PUT(req:Request){try{const b=await req.json();db.prepare('UPDATE settings SET shop_name=?,phone=?,address=?,logo=?,currency=?,invoice_footer=?,paper_size=?,updated_at=? WHERE id=1').run(String(b.shopName||'محل البصريات').trim(),String(b.phone||'').trim()||null,String(b.address||'').trim()||null,String(b.logo||'').trim()||null,String(b.currency||'ر.ي').trim(),String(b.invoiceFooter||'شكرًا لزيارتكم').trim(),b.paperSize==='THERMAL'?'THERMAL':'A4',new Date().toISOString());return GET()}catch(e){console.error(e);return NextResponse.json({success:false,error:'تعذر حفظ الإعدادات'},{status:500})}}

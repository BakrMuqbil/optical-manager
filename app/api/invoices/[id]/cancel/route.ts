import {NextResponse} from 'next/server';import {db} from '@/lib/db';export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const result=db.prepare("UPDATE invoices SET status='CANCELLED',updated_at=? WHERE id=? AND status<>'CANCELLED'").run(new Date().toISOString(),id);if(!result.changes)return NextResponse.json({success:false,error:'الفاتورة غير موجودة أو ملغاة'},{status:404});return NextResponse.json({success:true})}

import { NextResponse } from 'next/server'
import { db, normalizeOptionalInt, normalizeOptionalNumber, uid } from '@/lib/db'
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;return NextResponse.json({success:true,data:db.prepare('SELECT * FROM exams WHERE customer_id=? ORDER BY exam_date DESC,created_at DESC').all(id)})}
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;const b=await req.json();const exists=db.prepare('SELECT id FROM customers WHERE id=?').get(id);if(!exists)return NextResponse.json({success:false,error:'العميل غير موجود'},{status:404});const examId=uid('exam');db.prepare(`INSERT INTO exams (id,customer_id,exam_date,od_sph,od_cyl,od_axis,od_add,od_prism,od_base,os_sph,os_cyl,os_axis,os_add,os_prism,os_base,pd,near_pd,notes,examiner) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).run(examId,id,b.examDate||new Date().toISOString().slice(0,10),normalizeOptionalNumber(b.odSph),normalizeOptionalNumber(b.odCyl),normalizeOptionalInt(b.odAxis),normalizeOptionalNumber(b.odAdd),normalizeOptionalNumber(b.odPrism),b.odBase||null,normalizeOptionalNumber(b.osSph),normalizeOptionalNumber(b.osCyl),normalizeOptionalInt(b.osAxis),normalizeOptionalNumber(b.osAdd),normalizeOptionalNumber(b.osPrism),b.osBase||null,normalizeOptionalNumber(b.pd),normalizeOptionalNumber(b.nearPd),b.notes?.trim()||null,b.examiner?.trim()||null);return NextResponse.json({success:true,data:{id:examId}},{status:201})}

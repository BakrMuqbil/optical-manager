'use client'
import Link from 'next/link';import {useEffect,useState} from 'react';import {Search,Plus,Users,Eye,Receipt,ArrowLeft} from 'lucide-react';import {PageContainer} from '@/components/layout/PageContainer'
type Customer={id:string;customer_number:string;name:string;phone:string|null;address:string|null;exams_count:number;invoices_count:number;created_at:string}
export default function Page(){const [rows,setRows]=useState<Customer[]>([]),[q,setQ]=useState(''),[loading,setLoading]=useState(true);useEffect(()=>{const t=setTimeout(async()=>{setLoading(true);const r=await fetch(`/api/customers?q=${encodeURIComponent(q)}`);const j=await r.json();setRows(j.data||[]);setLoading(false)},200);return()=>clearTimeout(t)},[q]);return <PageContainer title="العملاء" description="ابحث عن العميل وافتح ملفه لمراجعة الفحوصات والفواتير" actions={<Link href="/customers/new" className="btn-primary"><Plus size={18}/>إضافة عميل</Link>}><div className="mb-5 flex items-center gap-3 rounded-2xl border border-slate-200 bg-white p-3 shadow-sm"><Search size={20} className="text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث بالاسم أو رقم الهاتف أو رقم العميل..." className="w-full bg-transparent py-2 text-sm outline-none"/></div><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">{loading?<div className="p-12 text-center text-sm text-slate-400">جاري تحميل العملاء...</div>:rows.length===0?<div className="p-12 text-center"><Users className="mx-auto text-slate-300" size={42}/><p className="mt-3 font-bold text-slate-700">لا توجد نتائج</p><p className="mt-1 text-sm text-slate-400">أضف أول عميل أو غيّر عبارة البحث.</p></div>:<div className="divide-y divide-slate-100">{rows.map(c=><Link href={`/customers/${c.id}`} key={c.id} className="flex flex-col gap-4 p-4 transition hover:bg-slate-50 md:flex-row md:items-center md:justify-between"><div><div className="flex items-center gap-3"><div className="flex h-10 w-10 items-center justify-center rounded-full bg-slate-100 font-black text-slate-700">{c.name.charAt(0)}</div><div><p className="font-black text-slate-900">{c.name}</p><p className="mt-1 text-xs text-slate-400">{c.customer_number} • {c.phone||'بدون هاتف'}</p></div></div></div><div className="flex items-center gap-5 text-xs text-slate-500"><span className="flex items-center gap-1"><Eye size={15}/> {c.exams_count} فحص</span><span className="flex items-center gap-1"><Receipt size={15}/> {c.invoices_count} فاتورة</span><ArrowLeft size={17} className="text-slate-300"/></div></Link>)}</div>}</div></PageContainer>}

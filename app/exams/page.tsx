'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {Eye,Plus} from 'lucide-react';import {PageContainer} from '@/components/layout/PageContainer';import {formatDate} from '@/lib/utils/formatDate';import type {Exam} from '@/types/app'
export default function Page(){const [rows,setRows]=useState<Exam[]>([]);useEffect(()=>{fetch('/api/exams').then(r=>r.json()).then(j=>setRows(j.data||[]))},[]);return <PageContainer title="فحوصات النظر" description="سجل الفحوصات المحفوظ لجميع العملاء" actions={<Link href="/exams/new" className="btn-primary"><Plus size={18}/>فحص جديد</Link>}><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="divide-y divide-slate-100">{rows.length?rows.map(e=><Link href={`/exams/${e.id}`} key={e.id} className="flex items-center justify-between p-5 hover:bg-slate-50"><div className="flex items-center gap-3"><span className="rounded-xl bg-indigo-50 p-3 text-indigo-600"><Eye size={20}/></span><div><p className="font-black">{e.customer_name}</p><p className="mt-1 text-xs text-slate-400">{e.exam_number} • {e.customer_number} • {formatDate(e.exam_date)}</p></div></div><div className="text-left text-xs text-slate-500">OD: {e.od_sph??'—'} / {e.od_cyl??'—'}<br/>OS: {e.os_sph??'—'} / {e.os_cyl??'—'}</div></Link>):<div className="p-12 text-center text-sm text-slate-400">لا توجد فحوصات.</div>}</div></div></PageContainer>}
